Remember to create symlinks to useful but untracked files/folders like `tsconfig.json`, `package.json`, `node_modules`, and perhaps `.vscode`.
Use `mklink "<name of symlink to create>" "<directory or file to link to>"`.
