import { WestHirthefordPassageName } from "../../../enums";
import { addPassage } from "../../../functions/passage_funcs";

addPassage(
	WestHirthefordPassageName.BACKUP,
	[],
	"You're not meant to be here. Something went wrong with the transversal >~<"
);
