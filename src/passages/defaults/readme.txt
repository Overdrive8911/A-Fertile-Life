- This just stores the passages that will be loaded in locations under normal conditions (e.g no cutscenes).

- All map areas that the player can enter must have a corresponding passage.

- All passages must be tagged with "default" as well as the "location_" and optionally "subLocation_" tags